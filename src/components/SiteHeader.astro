---
import { Menu } from "@lucide/astro";
---

<header class="site-header">
  <div class="site-shell header-inner">
    <a class="brand" href="/" aria-label="BDSMTest.top home">
      <span class="brand-mark" aria-hidden="true"><i></i><b></b></span>
      <span>BDSMTest<span>.top</span></span>
    </a>

    <nav class="desktop-nav" aria-label="Primary navigation">
      <a href="/methodology/">How scoring works</a>
      <a href="/bdsm-roles/">Roles</a>
      <a href="/consent-and-safety/">Consent & safety</a>
      <a class="nav-cta" href="/#start">Take the test</a>
    </nav>

    <details class="mobile-nav">
      <summary aria-label="Open navigation"><Menu size={22} aria-hidden="true" /></summary>
      <nav aria-label="Mobile navigation">
        <a href="/methodology/">How scoring works</a>
        <a href="/bdsm-roles/">Roles</a>
        <a href="/consent-and-safety/">Consent & safety</a>
        <a href="/#start">Take the test</a>
      </nav>
    </details>
  </div>
</header>

<style>
  .site-header {
    position: relative;
    z-index: 20;
    border-bottom: 1px solid var(--line);
    background: rgb(248 245 238 / 92%);
    backdrop-filter: blur(14px);
  }

  .header-inner {
    display: flex;
    min-height: 4.5rem;
    align-items: center;
    justify-content: space-between;
  }

  .brand {
    display: inline-flex;
    align-items: center;
    gap: 0.7rem;
    font-family: var(--font-display);
    font-size: 1.3rem;
    font-weight: 600;
    text-decoration: none;
  }

  .brand > span:last-child > span {
    color: var(--oxblood);
  }

  .brand-mark {
    position: relative;
    display: grid;
    width: 1.7rem;
    height: 1.7rem;
    place-items: center;
    border: 1.5px solid var(--ink);
    border-right-color: transparent;
    border-radius: 50%;
    transform: rotate(-18deg);
  }

  .brand-mark::before,
  .brand-mark::after,
  .brand-mark i {
    position: absolute;
    content: "";
  }

  .brand-mark::before,
  .brand-mark::after {
    width: 0.34rem;
    height: 0.34rem;
    border-radius: 50%;
    background: var(--oxblood);
  }

  .brand-mark::before { top: 0.17rem; right: 0.05rem; }
  .brand-mark::after { right: 0.03rem; bottom: 0.2rem; }

  .brand-mark i {
    width: 1.25rem;
    height: 1px;
    background: var(--oxblood);
    transform: rotate(70deg);
  }

  .brand-mark b { display: none; }

  .desktop-nav {
    display: none;
  }

  .mobile-nav {
    position: relative;
  }

  .mobile-nav summary {
    display: grid;
    width: 3rem;
    height: 3rem;
    place-items: center;
    border: 1px solid var(--line);
    border-radius: 50%;
    list-style: none;
    cursor: pointer;
  }

  .mobile-nav summary::-webkit-details-marker {
    display: none;
  }

  .mobile-nav nav {
    position: absolute;
    top: calc(100% + 0.5rem);
    right: 0;
    display: grid;
    width: min(18rem, calc(100vw - 2rem));
    padding: 0.5rem;
    border: 1px solid var(--line);
    border-radius: var(--radius);
    color: var(--paper);
    background: var(--night-raised);
    box-shadow: var(--shadow);
  }

  .mobile-nav nav a {
    padding: 0.85rem;
    text-decoration: none;
  }

  .mobile-nav nav a + a { border-top: 1px solid var(--line-dark); }

  @media (min-width: 52rem) {
    .desktop-nav {
      display: flex;
      align-items: center;
      gap: 1.75rem;
      font-size: 0.9rem;
      font-weight: 600;
    }

    .desktop-nav a {
      text-decoration: none;
    }

    .nav-cta {
      padding: 0.55rem 0.8rem;
      border: 1px solid var(--line-strong);
      border-radius: var(--radius);
    }

    .mobile-nav {
      display: none;
    }
  }
</style>
